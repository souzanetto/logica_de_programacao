algoritmo "semnome"
var
    cont, num, soma:inteiro
inicio
       num <- 1
       soma <- 0

       Escreva(soma)
       para cont <- 0 ate 6 faca
       num <- num + soma
       soma <- soma + num
       Escreva(num, soma)
       FimPara
fimalgoritmo
